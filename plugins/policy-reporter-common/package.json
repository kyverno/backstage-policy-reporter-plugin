{
  "name": "backstage-plugin-policy-reporter-common",
  "description": "Common functionalities for the policy-reporter plugin",
  "version": "0.2.2",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "Apache-2.0",
  "backstage": {
    "role": "common-library",
    "pluginId": "policy-reporter",
    "pluginPackages": [
      "backstage-plugin-policy-reporter",
      "backstage-plugin-policy-reporter-backend",
      "backstage-plugin-policy-reporter-common"
    ]
  },
  "sideEffects": false,
  "scripts": {
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean",
    "prepack": "backstage-cli package prepack",
    "postpack": "backstage-cli package postpack"
  },
  "devDependencies": {
    "@backstage/cli": "^0.29.2"
  },
  "files": [
    "dist"
  ]
}
